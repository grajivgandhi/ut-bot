git config user.name "Your Name"
git config user.email "your@email.com"
git add .
git commit -m "Initial commit"
git branch -M main
git push -u origin main
git add .
git commit -m "Update code"
git push origin main
git config user.name "Your Name"
git config user.email "your@email.com"
git add .
git commit -m "Initial commit"
git branch -M main
git push -u origin main
This file was moved to `archive/deployment/FREE_DEPLOYMENT.md`.

If you want to permanently delete the archived copy, remove `archive/deployment/FREE_DEPLOYMENT.md`.
